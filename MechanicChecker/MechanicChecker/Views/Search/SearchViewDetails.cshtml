@model SellerProduct;


<link href="~/css/StyleSeachViewDetails.css" rel="stylesheet" />
<link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">

<div class="wrapper w3-card w3-margin">

    <div class="w3-content" style="padding-bottom: 60px">
        <br />
        <br />
        <a onclick="history.back()" style="cursor: pointer;" class="previous">&laquo; Previous</a>
        <br />
        <br />
        <div style="margin-top: 10px;">
            <h1>@Model.localProduct.Title</h1>
        </div>
        <br />
        <span><img src="@Model.seller.CompanyLogoUrl" style="float: right; margin-left: 20px; margin-right: -30px" height="200" width="200" class="rg"></span>
        <table>
            <tr>
                <td>
                    <img src="@Model.localProduct.ImageUrl" style="border:solid; height:300px; margin-right: 30px;" alt="@Model.localProduct.Title" />
                    <br />
                </td>

                <td>&nbsp;</td>
                <td style="vertical-align: top; text-align:left;">
                    @if (Model.localProduct.Description != "")
                    {
                        <b>Description:</b>
                        <br />
                        <span>@Model.localProduct.Description</span>
                    }

                    @if (Model.localProduct.Category != "")
                    {
                        <span><br /><b>Category:</b> @Model.localProduct.Category </span>
                    }

                    @if (Model.localProduct.Price != "")
                    {
                        <span><br /><b>Price:</b> @Model.localProduct.Price</span>
                    }

                    @if (Model.distance != null)
                    {
                        <span><br /><b>Distance:</b> @(Model.distance)</span>
                    }

                    @if (Model.seller.CompanyName != "")
                    {
                        <span><br /><b>Company Name:</b> @Model.seller.CompanyName</span>
                    }

                    @if (Model.sellerAddress.Address != "")
                    {
                        <span><br /><b>Company Address:</b> @Model.sellerAddress.Address</span>
                    }

                    @if (Model.seller.BusinessPhone != "")
                    {
                        <span><br /><b>Phone:</b> @Model.seller.BusinessPhone</span>
                    }

                    @if (Model.seller.AccountType != "")
                    {
                        <span><br /><b>Account Type:</b> @Model.seller.AccountType</span>
                    }
                    <br />
                    <br />
                    @if (Model.localProduct.ProductUrl != "")
                    {
                        <span><a href="@Model.localProduct.ProductUrl" class="btn btn-primary">Product link</a></span>
                    }

                    @if (Model.seller.WebsiteUrl != "")
                    {
                        <span><a href="@Model.seller.WebsiteUrl" target="_blank" class="btn btn-primary">Company Web Site</a></span>
                    }
                    <br />
                    <br />
                </td>
            </tr>
        </table>
    </div>
</div>
